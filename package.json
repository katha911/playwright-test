{
  "name": "playwright-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "playwright:runtest": "npx playwright test",
    "playwright:debug": "npx playwright test --debug",
    "playwright:codegen": "npx playwright codegen",
    "playwright:trace-on": "npx playwright test --trace on"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.28.1"
  },
  "dependencies": {
    "playwright-merge-html-reports": "^0.2.6"
  }
}
